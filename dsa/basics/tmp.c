
//main(_){for(--_;putchar(_++["J!Mpwf!Zpv\1"]-1););}
//
#include <stdio.h>

#define MAX(a, b) a > b ? a : b
typedef struct Point
{
	    int x;
	    int y;
} Point;


int main()
{

	//int n = 3;
	int x, *b, *c, *a;

	//x = MAX(2, n++);
	//printf("%d  %d", x, n);

	Point *p = NULL;
	a = &x;
	b = a + 1;
	int q = &(p->y);

	printf("%d  \n", q);
	return 0;
}
